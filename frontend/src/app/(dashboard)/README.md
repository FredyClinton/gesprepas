# (dashboard)

Espace connecté. Chaque sous-dossier correspond à un module métier et contiendra ses
propres `page.tsx` (routing fin, composant du module).

Un `layout.tsx` sera ajouté ici (sidebar + sélecteur de rôle actif, communs à tout
l'espace connecté) lors de l'implémentation des écrans métier — voir `frontend/CLAUDE.md`.
